{
  "name": "sistema-mensagem",
  "private": true,
  "version": "0.1.0",
  "description": "SEEG Omni — plataforma omnichannel de mensagens + CRM conversacional",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}
